SELECT
  DISTINCT
  a.AGENT_ID,
  a.PARENT_ID,
  a.PLATFORM_ID,
  a.DISPLAY_NAME,
  a.DESCRIPTION
FROM
  AGENT a
  INNER JOIN
  RESOURCE res
    ON
      a.AGENT_ID = res.AGENT_ID
WHERE
  a.IS_DELETED = 0